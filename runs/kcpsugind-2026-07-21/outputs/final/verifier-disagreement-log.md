# Verifier disagreement log — KCPSUGIND 2026-07-21

One row per point where a downstream step's conclusion conflicted with a Verifier A (B12a) source-fidelity finding: a re-derivation that leaned on a flagged number, an inclination to keep a figure Verifier A flagged, or a source re-check that cleared a flag.

Fixed shape: Date | Run (ticker-date) | Number/claim | Verifier A verdict + anchor | Downstream step + its position | Disposition | Note

Verifier A (B12a) returned 0 CRITICAL, 0 MAJOR, and 0 source-fidelity MISMATCH across 47 figures checked. The single MINOR finding is a rounding-tolerance note on the distillery decline (-82.25% stated as -82.3% / -82%), not a source-fidelity flag, and no downstream step (Verifier C re-derivation, narrative, or valuation) leaned on, kept, or re-cleared any flagged figure in conflict with Verifier A.

The operator Eimco 15x override (2026-07-21) that moved the decision to WATCHLIST is an operator ruling on an approved valuation input, not a downstream re-derivation leaning on a Verifier A finding; it creates no disagreement row.

none
