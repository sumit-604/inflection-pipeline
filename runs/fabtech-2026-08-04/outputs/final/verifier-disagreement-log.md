# Verifier Disagreement Log — Fabtech Technologies Ltd (FABTECH) | Run 2026-08-04

One row per point where a downstream step's conclusion conflicted with a Verifier A source-fidelity finding (a re-derivation leaning on a flagged number, an inclination to keep a figure Verifier A flagged, or a source re-check that cleared a flag).

Verifier A (B12a) logged 0 source-fidelity MISMATCHes this run (source_fidelity_mismatches: 0). Both B12a CRITICALs are source-internal inconsistencies or concall-silence items correctly surfaced by the stages, not flagged stage figures that any downstream step reasoned around. No downstream step carried a Verifier A flagged figure into a verdict card, table or payload as valid.

none
